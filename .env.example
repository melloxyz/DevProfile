# Publica (nao sensivel)
NEXT_PUBLIC_GITHUB_USERNAME=melloxyz

# Server-only (nunca use NEXT_PUBLIC_ para segredos)
GITHUB_TOKEN=
ADMIN_ROUTE_SLUG=studio-74f1c8
# Use hash Argon2id. Em .env, escape "$" como "\$" para evitar expansao de variavel.
# Exemplo de formato: \$argon2id\$v=19\$m=65536,t=3,p=4\$SALT\$HASH
ADMIN_PASSWORD_HASH=
ADMIN_SESSION_SECRET=
KV_REST_API_URL=
KV_REST_API_TOKEN=
